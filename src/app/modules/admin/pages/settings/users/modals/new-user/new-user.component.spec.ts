import { NewUserComponent } from './new-user.component';

describe('NewUserComponent', () => {
  let component: NewUserComponent;

  beforeEach(() => {
    component = new NewUserComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
