import { ValidateInvoiceComponent } from './validate-invoice.component';

describe('ValidateInvoiceComponent', () => {
  let component: ValidateInvoiceComponent;

  beforeEach(() => {
    component = new ValidateInvoiceComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
